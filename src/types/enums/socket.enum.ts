export enum SocketDisconnectReason {
  "server namespace disconnect" = "server namespace disconnect",
  "client namespace disconnect" = "client namespace disconnect",
  "server shutting down" = "server shutting down",
  "ping timeout" = "ping timeout",
  "transport close" = "transport close",
  "transport error" = "transport error",
  "parse error" = "parse error",
  "forced close" = "forced close",
  "forced server close" = "forced server close",
}
